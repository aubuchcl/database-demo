{
  "name": "database-demo-ui",
  "version": "1.0.0",
  "private": true,
  "description": "Public dashboard for the Cycle database demo. Reads from Postgres over the environment's private network.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "pg": "^8.23.0"
  }
}
